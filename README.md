## MY CUSTOM AI CHAT BOT
This is for IT Coworker
Keep develop the function and add new skills.

### Local secrets setup
- Streamlit normally reads `.streamlit/secrets.toml`.
- This app now also falls back to `secrets.toml`, `.streamlit/secrets.toml.example`, and `secrets.toml.example` for local development.
- Copy `secrets.toml.example` to `.streamlit/secrets.toml` if you want the standard Streamlit behavior.
